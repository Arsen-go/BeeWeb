const { userInput } = require("./userInput");
const { workspaceInput } = require("./workspaceInput");
const { filterInput } = require("./filterInput");
const { conversationInput} = require("./conversationInput");

module.exports = {
    userInput, workspaceInput, filterInput, conversationInput
};